@page
@model Dot_NET_CRUD_Project.Pages.BuildingsPage.IndexModel

@{
    ViewData["Title"] = "Buildings";
}

<div class="row g-3">
    <a class="btn" asp-page="Create">Create New </a>
</div>
<br>

<div class="row table-wrapper-scroll-y custom-scrollbar table-wrapper-scroll-x">
    <table class="table table-responsive shadow">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Buildings[0].Building)
                </th>
                <th style="display: flex; justify-content: center;">Options</th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model.Buildings) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Building)
                </td>
                    <td td style="display: flex; justify-content: center;">
                    <a class="btn" asp-page="./Edit" asp-route-id="@item.PKBuilding">Edit</a> 
                    <a class="btn"asp-page="./Delete" asp-route-id="@item.PKBuilding">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>
</div>